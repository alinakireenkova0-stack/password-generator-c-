name: Fast Build

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Компиляция
      run: g++ generator/main.cpp -o app

    - name: Запуск
      run: ./app
