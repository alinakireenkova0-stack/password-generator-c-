version: '3.8'

services:
  service1:
    build: ./service1
    ports:
      - "5000:5000"
    environment:
      - SERVICE2_URL=http://service2:5001
    depends_on:
      - service2

  service2:
    build: ./service2
    ports:
      - "5001:5001"